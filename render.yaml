# render.yaml
services:
  - type: web
    name: peter-md-session
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node index.js"
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
